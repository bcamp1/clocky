Make a python executable `clocky` which calls `clock` (source code located in ../clock/) with the correct arguments over ssh profile `chum`

`clocky <options>` -> `ssh chum 'clock <options>'`

and it will print the output to the client.

